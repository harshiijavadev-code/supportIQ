# App name
spring.application.name=support-IQ

# Server
server.port=8080

# Render PostgreSQL Database
spring.datasource.url=jdbc:postgresql://dpg-d6vbp4ua2pns73ag6ivg-a.oregon-postgres.render.com:5432/supportiq
spring.datasource.username=harsh
spring.datasource.password=cTXvWsrHkt0wrPhgAvTRZbPvczjFQhpJ
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=supportIQSecretKey2024ThisIsAVeryLongSecretKeyThatIsAtLeast256BitsLongForSecurity
jwt.expiration=36000000


---

## ? **The Difference:**

### **PostgreSQL Format (Wrong for Spring Boot):**
```
postgresql://username:password@host/database


### **JDBC Format (Correct for Spring Boot):**
jdbc:postgresql://host:port/database